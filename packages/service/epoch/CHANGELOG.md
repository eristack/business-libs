# @eristack/epoch

## 0.1.1

### Patch Changes

- 294445c: Add `bumpMany(scopes)` for multi-scope cache invalidation after writes.

## 0.1.0

### Minor Changes

- 237f114: Initial release of `@eristack/epoch`: headless per-scope data-version counters, cache policy (`use-cache` / `refetch`), Drizzle store, REST/Express/Nest/client/React/Backseat adapters.

### Patch Changes

- 7847ca5: Add MIT license: root `LICENSE`, per-package `LICENSE` in publish tarball, and `"license": "MIT"` in `package.json`.

## 0.0.0

- Initial release: headless epoch counters, cache policy resolution, Drizzle + REST + Express + Nest + client + React + Backseat adapters.

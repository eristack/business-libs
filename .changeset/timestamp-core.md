---
"@eristack/timestamp": minor
---

Initial `@eristack/timestamp`: instant mode (UTC facts + IANA zone), wall mode (local intent, DST-safe), Temporal polyfill, `TimestampJSON` wire shape, and full adapter spine (`/drizzle`, `/rest`, `/zod`, `/express`, `/nest`, `/client`, `/react`) mirroring `@eristack/money`.

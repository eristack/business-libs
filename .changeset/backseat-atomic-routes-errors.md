---
"@eristack/backseat": patch
---

Add `store.atomic()` for multi-collection document writes. Add `listRoutes()`, `routesSnapshot()`, and Devtools Routes export for Horizon B derivation. Add `jsonError()`, `versionConflict()`, and `BackseatVersionConflictError` for standard error envelopes.

---
"@eristack/jwt-auth": minor
---

Add username/password credentials (scrypt) as a child of app users via `jwt_auth_credentials`, session listing/revoke-by-id, and tighter adapter DI (injected storage/baseUrl, Nest `registerAsync`).

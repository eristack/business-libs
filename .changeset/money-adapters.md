---
"@eristack/money": minor
---

Add adapter subpaths (`./drizzle`, `./rest`, `./zod`, `./express`, `./nest`, `./client`, `./react`). Move implementation under `src/core/`; root export unchanged. Zod 4 only (`peer: zod ^4`). Per-subpath docs.

---
"@eristack/money": patch
---

Add `parseRoundedAmount` and amount-only TanStack Form helpers (`createAmountOnlyFieldValidators`, `submitAmountOnlyFormValue`) for shared-currency ERP/QUPS line fields.

---
"@eristack/ai-knowledge": patch
---

Add `file-upload-s3` recipe and catalog entries for `@eristack/file-manager`. Extend `dev-conventions` with the new-package shipping checklist (package docs, recipes, `apps/web` ecosystem/products, `docs:sync`). Update `upgrading`, `document-lines-erp` (attachments + IndexedDB row), and mirrored site guides.

---
"@eristack/backseat": patch
---

Publish `./adapters` export (`registerRestLikeRoutes`, `asDate`, `asNullableDate`, `toRestLikeRequest`) required by spine `./backseat` packages. Fixes Vite `Missing "./adapters" specifier` on `@eristack/backseat@0.1.0`.

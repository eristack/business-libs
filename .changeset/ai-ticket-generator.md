---
"@eristack/ai-ticket-generator": minor
---

Add `@eristack/ai-ticket-generator`: portable bug/suggestion tickets, feasibility gate, mandatory `ticket.yaml` subscription (`pnpm ticket:check`), and `eristack-ticket` CLI for maintainer handoff files.
